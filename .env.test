# Environment variables for E2E tests
# Copy this file to .env.test and adjust if necessary
# This file is used when running E2E tests with Docker

# IMPORTANT: DATABASE_URL must be DIFFERENT from .env
# - .env points to development database (port 5432)
# - .env.test points to isolated test database in Docker (port 5433)
# This ensures tests don't affect the development database
DATABASE_URL=postgresql://prisma:prisma@localhost:5433/tests?schema=public
DIRECT_URL=postgresql://prisma:prisma@localhost:5433/tests?schema=public
# API_KEY can be the same or different from .env
# Using a different value helps distinguish between environments
API_KEY=test-api-key

PORT=3009
NODE_ENV=test

